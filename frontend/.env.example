# mock 인증 사용 여부 (true면 백엔드 호출 없이 프론트 가짜 응답 사용)
VITE_USE_MOCK_AUTH=false

# 프론트에서 호출할 API 기본 경로
VITE_API_BASE_URL=/backend

# Vite 개발 서버 프록시 사용 여부
DEV_USE_PROXY=true

# 프록시할 요청 경로 prefix
DEV_PROXY_PREFIX=/backend

# 프록시가 전달할 실제 백엔드 주소(XAMPP)
DEV_PROXY_TARGET=http://localhost/TONE
